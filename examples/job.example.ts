
/* path: src/jobs/example.ts
import { Job } from "../common/interfaces/job";

export default class ExampleJob implements Job {
  public date: string = "* * * * *";
  public enabled: boolean = true;
  public execute = async (): Promise<void> => {};
}
*/
